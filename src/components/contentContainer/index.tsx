import styled from 'styled-components'

export const ContentContainer = styled.div`
  max-width: 180rem;
  min-height: 100vh;
  margin: auto;
  display: flex;
  flex-direction: column;
  position: relative;
`
